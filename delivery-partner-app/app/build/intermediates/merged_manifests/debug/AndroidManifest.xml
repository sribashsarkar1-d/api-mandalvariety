<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mandalvariety.deliverypartner"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <permission
        android:name="com.mandalvariety.deliverypartner.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.mandalvariety.deliverypartner.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" /> <!-- Additional permissions only if strictly required by website functionality (e.g., calling) -->
    <!-- The user explicitly said: "Request only the permissions that are actually necessary. Do NOT request unnecessary permissions such as Contacts, SMS, Camera, Microphone, Location." -->
    <!--
 Using tel: intent does not strictly require CALL_PHONE permission if using ACTION_DIAL, 
         but if ACTION_CALL is used it does. ACTION_DIAL is safer and requires no permission.
    -->
    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:icon="@drawable/logo_placeholder"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_placeholder"
        android:supportsRtl="true"
        android:theme="@style/Theme.DeliveryPartner"
        android:usesCleartextTraffic="false" > <!-- Security: Do not allow cleartext HTTP unless needed -->

        <activity
            android:name="com.mandalvariety.deliverypartner.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DeliveryPartner.Splash" > <!-- Splash screen theme -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.mandalvariety.deliverypartner.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>